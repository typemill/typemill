---
description: Create a new theme skeleton
---

Create a new Theme skeleton named $ARGUMENTS in the /themes folder.

Include basic structure with a yaml file for configurations, twig files, and a public folder with tachyons css library and javascript files if needed.

Follow the conventions in @ai/docs/theme-development.md.