{% extends 'layouts/layoutAuth.twig' %}

{% block title %}{{ translate('Login Verification Code') }}{% endblock %}

{% block content %}

	<div class="flex flex-col-reverse min-h-screen justify-end {{ authpagestacked ? '' : 'lg:flex-row lg:min-h-0' }}">
		
		<div class="flex-1 flex justify-center items-center {{ authpagestacked ? 'w-full' : 'lg:w-1/2 lg:flex-none lg:min-h-screen' }}" style="background-color: {{ authpagebgcolor|default('#0d9488') }}; color: {{ authpagetextcolor|default('#ffffff') }};">
			<div class="max-w-md content-center">
			
				<p>{{ translate('Enter the verification code from your email:') }}</p>

				<form method="POST" action="{{ url_for("auth.authcode") }}" autocomplete="off">
				
					<fieldset>

						<div class="flex justify-between my-2">
							<input 
								type="text" 
								inputmode="numeric"
								name="code-1" 
								pattern="[0-9]" 
								maxlength="1" 
								oninput="moveToNextField(this)"	
								class="mr-2 form-control block w-full px-3 py-3 text-xl text-center font-bold text-gray-700 bg-white bg-clip-padding border border-solid border-gray-300 transition ease-in-out m-0 focus:text-gray-700 focus:bg-white focus:border-blue-600 focus:outline-non"
							required>
							<input 
								type="text" 
								inputmode="numeric"
								name="code-2" 
								pattern="[0-9]" 
								maxlength="1" 
								oninput="moveToNextField(this)"	
								class="mr-2 form-control block w-full px-3 py-3 text-xl text-center font-bold text-gray-700 bg-white bg-clip-padding border border-solid border-gray-300 transition ease-in-out m-0 focus:text-gray-700 focus:bg-white focus:border-blue-600 focus:outline-non"
							required>
							<input 
								type="text" 
								inputmode="numeric"
								name="code-3" 
								pattern="[0-9]" 
								maxlength="1" 
								oninput="moveToNextField(this)"	
								class="mr-2 form-control block w-full px-3 py-3 text-xl text-center font-bold text-gray-700 bg-white bg-clip-padding border border-solid border-gray-300 transition ease-in-out m-0 focus:text-gray-700 focus:bg-white focus:border-blue-600 focus:outline-non"
							required>
							<input 
								type="text" 
								inputmode="numeric"
								name="code-4" 
								pattern="[0-9]" 
								maxlength="1" 
								oninput="moveToNextField(this)"	
								class="mr-2 form-control block w-full px-3 py-3 text-xl text-center font-bold text-gray-700 bg-white bg-clip-padding border border-solid border-gray-300 transition ease-in-out m-0 focus:text-gray-700 focus:bg-white focus:border-blue-600 focus:outline-non"
							required>
							<input 
								type="text" 
								inputmode="numeric"
								name="code-5" 
								pattern="[0-9]" 
								maxlength="1" 
								oninput="moveToNextField(this)"	
								class="mr-2 form-control block w-full px-3 py-3 text-xl text-center font-bold text-gray-700 bg-white bg-clip-padding border border-solid border-gray-300 transition ease-in-out m-0 focus:text-gray-700 focus:bg-white focus:border-blue-600 focus:outline-non"
							required>
							<input 
								type="text" 
								inputmode="numeric"
								name="code-6" 
								pattern="[0-9]" 
								maxlength="1" 
								oninput="moveToNextField(this)"	
								class="form-control block w-full px-3 py-3 text-xl text-center font-bold text-gray-700 bg-white bg-clip-padding border border-solid border-gray-300 transition ease-in-out m-0 focus:text-gray-700 focus:bg-white focus:border-blue-600 focus:outline-non"
							required>
						</div>

						<input type="hidden" value="{{username|e}}" name="username">

						<div class="personal-mail hidden" style="display:none">
							<label>Personal Mail</label>
							<input type="text" name="personal-honey-mail">
						</div>

						<input 
							type="submit" 
							value="{{ translate('Confirm') }}" 
							style="color: {{ authpagetextcolor|default('#ffffff') }}; border-color: {{ authpagetextcolor|default('#ffffff') }};"
                       		class="block w-full mt-6 px-3 py-3 border-0 font-medium text-xs leading-tight uppercase border-2 pointer auth-btn cursor-pointer focus:outline-none focus:ring-0 transition duration-100 ease-in-out"
						/>

					</fieldset>
					<script>
					  function moveToNextField(currentField) {
					    var inputValue = currentField.value;
					    var inputLength = inputValue.length;
					    if (inputLength === 1)
					    {
					      var nextField = currentField.nextElementSibling;
					      if (nextField)
					      {
					        nextField.focus();
					      }
					    }
					  }						
					</script>
				</form>
			</div>
		</div>
		
		<div class="p-5 border-b border-white content-center flex justify-center items-center auth-right-side {{ authpagestacked ? 'w-full' : 'lg:w-1/2' }}">
			<div class="max-w-md content-center {{ authpagestacked ? 'text-center' : '' }}">
				<h1 class="text-4xl py-5">{{ authtitle|e }}</h1>
				<p class="py-2">{{ authtext|e }}</p>
				<a class="py-2" href="{{ url_for('auth.show') }}">&#x2192; {{ translate('Back to login') }}</a>
			</div>
		</div>
	
	</div>
{% endblock %}
